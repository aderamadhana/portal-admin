name: Deploy Portal Admin

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Production (No SSH)
    runs-on: ubuntu-latest

    steps:
      - name: 📋 Checkout Code
        uses: actions/checkout@v4

      - name: 📦 Install Dependencies (CI Check Only)
        run: |
          composer install --no-dev --no-interaction --prefer-dist --optimize-autoloader

      - name: 🧪 Laravel Sanity Check
        run: |
          php -v
          php artisan --version || true

      - name: ✅ Deployment Marked Successful
        run: |
          echo "========================================="
          echo "✅ Deployment pipeline completed"
          echo "ℹ️  No SSH / no server-side deployment executed"
          echo "🌐 Target: portal.aderamadhana.com"
          echo "🔖 Git commit: $GITHUB_SHA"
          echo "📅 Time: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "========================================="

      - name: 📣 GitHub Notice
        run: |
          echo "::notice title=Deploy Finished::Pipeline finished without SSH-based deployment"
